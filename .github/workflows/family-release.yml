# =============================================================================
# Family Skill Release Workflow
# =============================================================================
# 공개 repo에서 secrets가 주입된 비공개 스킬 ZIP을 가족에게 이메일로 전송
#
# 필요한 GitHub Secrets:
#   - BEOPSUNY_OC_CODE: 법령정보센터 OC 코드
#   - BEOPSUNY_ASSEMBLY_API_KEY: 국회 API 키 (선택)
#   - MAIL_USER: Gmail 주소
#   - MAIL_APP_PASSWORD: Gmail 앱 비밀번호 (2FA 필요)
#   - FAMILY_EMAILS: 수신자 이메일 (콤마 구분)
#
# Gmail 앱 비밀번호 생성:
#   1. https://myaccount.google.com/apppasswords
#   2. 2단계 인증 활성화 필요
#   3. "메일" + "기타(맞춤 이름)" 선택 → 16자리 비밀번호 생성
# =============================================================================

name: Family Skill Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: '버전 태그 (예: v1.0.0)'
        required: true
        type: string
      release_notes:
        description: '릴리즈 노트 (선택)'
        required: false
        type: string
        default: ''

# 보안: 최소 권한 원칙
permissions:
  contents: read

jobs:
  build-and-send:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # 보안: secrets를 echo하지 않고 파일에만 씀
      - name: Inject API credentials
        run: |
          cat > .claude/skills/beopsuny/config/.env <<'ENVEOF'
          # Auto-generated by GitHub Actions
          # DO NOT commit this file
          BEOPSUNY_OC_CODE=${{ secrets.BEOPSUNY_OC_CODE }}
          BEOPSUNY_ASSEMBLY_API_KEY=${{ secrets.BEOPSUNY_ASSEMBLY_API_KEY }}
          ENVEOF

          # .env 파일이 생성되었는지만 확인 (내용은 출력 안함)
          echo "✅ .env file created ($(wc -c < .claude/skills/beopsuny/config/.env) bytes)"

      - name: Create skill ZIP
        run: |
          cd .claude/skills

          # ZIP 생성 (불필요한 파일 제외)
          zip -r ../../beopsuny-skill-${{ inputs.version }}.zip beopsuny \
            -x "*.pyc" \
            -x "*/__pycache__/*" \
            -x "*.git*" \
            -x "*.DS_Store"

          cd ../..
          echo "📦 ZIP created: $(ls -lh beopsuny-skill-${{ inputs.version }}.zip | awk '{print $5}')"

      - name: Verify ZIP contents
        run: |
          # ZIP 내용 확인 (파일 목록만, 내용은 안봄)
          echo "📋 ZIP contents:"
          unzip -l beopsuny-skill-${{ inputs.version }}.zip | head -30
          echo "..."

          # .env가 포함되었는지 확인
          if unzip -l beopsuny-skill-${{ inputs.version }}.zip | grep -q ".env"; then
            echo "✅ .env file included in ZIP"
          else
            echo "❌ .env file missing!"
            exit 1
          fi

      - name: Send to family via email
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          secure: true
          username: ${{ secrets.MAIL_USER }}
          password: ${{ secrets.MAIL_APP_PASSWORD }}
          subject: "법순이 스킬 ${{ inputs.version }}"
          to: ${{ secrets.FAMILY_EMAILS }}
          from: ${{ secrets.MAIL_USER }}
          attachments: beopsuny-skill-${{ inputs.version }}.zip
          body: |
            법순이(Beopsuny) 스킬 ${{ inputs.version }} 배포
            ================================================

            ${{ inputs.release_notes || '새 버전이 배포되었습니다.' }}

            설치 방법
            ---------
            1. 첨부된 ZIP 파일 다운로드
            2. 압축 해제
            3. Claude Desktop → Settings → Skills → Import
            4. beopsuny 폴더 선택

            사용법
            ------
            - 법령 검색: "민법 제750조 알려줘"
            - 판례 검색: "손해배상 관련 판례 찾아줘"
            - 체크리스트: "스타트업 설립 체크리스트 보여줘"

            문의: GitHub Issues 또는 직접 연락

            ---
            자동 생성: GitHub Actions
            커밋: ${{ github.sha }}

      # 보안: 작업 후 민감 파일 정리
      - name: Cleanup sensitive files
        if: always()
        run: |
          rm -f .claude/skills/beopsuny/config/.env
          rm -f beopsuny-skill-*.zip
          echo "🧹 Cleanup completed"
